<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Call this template instead of "web.assets_tests" to have the proper conditional check -->
    <template id="conditional_assets_tests" name="Tests Assets Bundle">
        <t t-if="'tests' in debug or test_mode_enabled">
            <t t-if="ignore_missing_deps">
                <!-- FIXME: This is only to ignore the errors for the lazy loading. To allow all tests assets and tours to be in the same bundle, the assets_tests bundle ignores missing dependencies -->
                <t t-call-assets="web.__assets_tests_call__" defer_load="True" />
            </t>
            <t t-else="">
                <t t-call-assets="web.assets_tests" defer_load="True" />
            </t>
        </t>
    </template>

    <template id="web.layout" name="Web layout">&lt;!DOCTYPE html&gt;
        <html t-att="html_data or {}">
            <head>
                <meta charset="utf-8"/>
                <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
                <title t-esc="UniERP"/>
                <link type="image/x-icon" rel="shortcut icon" t-att-href="x_icon or '/web/static/img/favicon.ico'"/>
                <script id="web.layout.odooscript" type="text/javascript">
                    var odoo = {
                        csrf_token: "<t t-esc="request.csrf_token(None)"/>",
                        debug: "<t t-esc="debug"/>",
                    };
                </script>
                <t t-out="head or ''"/>
            </head>
            <body t-att-class="body_classname">
                <t t-out="0"/>
            </body>
        </html>
    </template>
    <template id="web.frontend_layout" name="Frontend Layout" inherit_id="web.layout" primary="True">
      <xpath expr="//head/meta[last()]" position="after">
        <meta name="viewport" content="width=device-width, initial-scale=1"/>
      </xpath>
      <xpath expr="//head/link[last()]" position="after">
        <link rel="preload" href="/web/static/src/libs/fontawesome/fonts/fontawesome-webfont.woff2?v=4.7.0" as="font" crossorigin=""/>
        <t t-call-assets="web.assets_frontend" t-js="false"/>
      </xpath>
      <xpath expr="//head/script[@id='web.layout.odooscript']" position="after">
        <script type="text/javascript">
            odoo.__session_info__ = <t t-out="json.dumps(request.env['ir.http'].get_frontend_session_info())"/>;
            if (!/(^|;\s)tz=/.test(document.cookie)) {
                const userTZ = Intl.DateTimeFormat().resolvedOptions().timeZone;
                document.cookie = `tz=${userTZ}; path=/`;
            }
        </script>
        <t t-call-assets="web.assets_frontend_minimal" t-css="false" defer_load="True"/>
        <t t-call="web.conditional_assets_tests" ignore_missing_deps="True"/>
        <t t-call-assets="web.assets_frontend_lazy" t-css="false" lazy_load="True"/>
      </xpath>
        <xpath expr="//t[@t-out='0']" position="replace">
            <div id="wrapwrap" t-attf-class="#{pageName or ''} glassmorphism-wrap">
                <header t-if="not no_header" id="top" data-anchor="true" class="glassmorphism-header">
                    <img class="img-responsive d-block mx-auto glass-logo"
                        t-attf-src="/web/binary/company_logo"
                        alt="Logo"/>
                </header>
                <main class="glassmorphism-main">
                    <t t-out="0"/>
                </main>
                <footer t-if="not no_footer" id="bottom" data-anchor="true" class="glassmorphism-footer o_footer">
                    <div id="footer"/>
                    <div t-if="not no_copyright" class="o_footer_copyright">
                        <div class="container py-3">
                            <div class="row row-gap-2">
                                <div class="col-sm text-center text-sm-start text-muted">
                                    <span class="o_footer_copyright_name me-2">Copyright &amp;copy; <span t-field="res_company.name" itemprop="name">Company name</span></span>
                                </div>
                                <div class="col-sm text-center text-sm-end">
                                    <t t-call="web.brand_promotion"/>
                                </div>
                            </div>
                        </div>
                    </div>
                </footer>
            </div>
        </xpath>
    </template>

    <template id="brand_promotion_message" name="Brand Promotion Message">
        <t t-set="odoo_logo">
            <a target="_blank"
                t-attf-href="http://www.odoo.com?utm_source=db&amp;utm_medium=#{_utm_medium}"
                class="badge text-bg-light">
                <img alt="Odoo"
                    src="/web/static/img/odoo_logo_tiny.png"
                    width="62" height="20"
                    style="width: auto; height: 1em; vertical-align: baseline;"/>
            </a>
        </t>
        <t t-set="final_message">Powered by %s%s</t>
        <t t-out="final_message % (odoo_logo, _message and ('- ' + _message) or '')"/>
    </template>
    <template id="brand_promotion" name="Brand Promotion">
        <div class="o_brand_promotion">
            <t t-call="web.brand_promotion_message">
                <t t-set="_message"></t>
                <t t-set="_utm_medium" t-valuef="portal"/>
            </t>
        </div>
    </template>

    <template id="web.login_layout" name="Login Layout">
        <t t-call="web.frontend_layout">
            <t t-set="html_data" t-value="{'style': 'height: 100%;'}"/>
            <t t-set="body_classname" t-value="'glassmorphism-bg'"/>
            <t t-set="no_header" t-value="True"/>
            <t t-set="no_footer" t-value="True"/>
            
            <style>
                .glassmorphism-bg {
                    background: linear-gradient(135deg, #667eea 0%, #764ba2 25%, #f093fb 50%, #feca57 75%, #48c9b0 100%);
                    background-size: 400% 400%;
                    animation: gradientShift 15s ease infinite;
                    min-height: 100vh;
                }
                
                @keyframes gradientShift {
                    0% { background-position: 0% 50%; }
                    50% { background-position: 100% 50%; }
                    100% { background-position: 0% 50%; }
                }
                
                .glass-card {
                    background: rgba(255, 255, 255, 0.15);
                    backdrop-filter: blur(20px);
                    -webkit-backdrop-filter: blur(20px);
                    border-radius: 20px;
                    border: 1px solid rgba(255, 255, 255, 0.2);
                    box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.37);
                    transition: all 0.3s ease;
                }
                
                .glass-card:hover {
                    transform: translateY(-5px);
                    box-shadow: 0 15px 40px 0 rgba(31, 38, 135, 0.45);
                }
                
                .glass-input {
                    background: rgba(255, 255, 255, 0.1) !important;
                    border: 1px solid rgba(255, 255, 255, 0.2) !important;
                    border-radius: 12px !important;
                    color: #333 !important;
                    backdrop-filter: blur(10px);
                    -webkit-backdrop-filter: blur(10px);
                    transition: all 0.3s ease !important;
                }
                
                .glass-input:focus {
                    background: rgba(255, 255, 255, 0.25) !important;
                    border-color: rgba(255, 255, 255, 0.4) !important;
                    box-shadow: 0 0 20px rgba(255, 255, 255, 0.3) !important;
                    outline: none !important;
                }
                
                .glass-input::placeholder {
                    color: rgba(51, 51, 51, 0.6) !important;
                }
                
                .glass-button {
                    background: linear-gradient(135deg, rgba(102, 126, 234, 0.8), rgba(118, 75, 162, 0.8)) !important;
                    border: 1px solid rgba(255, 255, 255, 0.2) !important;
                    border-radius: 12px !important;
                    color: white !important;
                    font-weight: 500 !important;
                    backdrop-filter: blur(10px);
                    -webkit-backdrop-filter: blur(10px);
                    transition: all 0.3s ease !important;
                    position: relative;
                    overflow: hidden;
                }
                
                .glass-button:hover {
                    background: linear-gradient(135deg, rgba(102, 126, 234, 0.9), rgba(118, 75, 162, 0.9)) !important;
                    transform: translateY(-2px);
                    box-shadow: 0 10px 25px rgba(102, 126, 234, 0.4) !important;
                }
                
                .glass-button::before {
                    content: '';
                    position: absolute;
                    top: 0;
                    left: -100%;
                    width: 100%;
                    height: 100%;
                    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
                    transition: left 0.5s;
                }
                
                .glass-button:hover::before {
                    left: 100%;
                }
                
                .glass-label {
                    color: #333 !important;
                    font-weight: 500 !important;
                    text-shadow: 0 1px 2px rgba(255, 255, 255, 0.5);
                }
                
                .glass-logo {
                    filter: drop-shadow(0 4px 8px rgba(0, 0, 0, 0.2));
                    transition: transform 0.3s ease;
                }
                
                .glass-logo:hover {
                    transform: scale(1.05);
                }
                
                .glass-divider {
                    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
                    height: 1px;
                }
                
                .widget-container {
                    display: flex;
                    justify-content: space-between;
                    margin-bottom: 1.5rem;
                    gap: 1rem;
                }
                
                .weather-widget, .date-widget {
                    background: rgba(255, 255, 255, 0.1);
                    backdrop-filter: blur(15px);
                    -webkit-backdrop-filter: blur(15px);
                    border-radius: 15px;
                    border: 1px solid rgba(255, 255, 255, 0.2);
                    padding: 0.75rem 1rem;
                    flex: 1;
                    transition: all 0.3s ease;
                    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
                }
                
                .weather-widget:hover, .date-widget:hover {
                    transform: translateY(-3px);
                    box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15);
                }
                
                .widget-title {
                    font-size: 0.75rem;
                    color: rgba(51, 51, 51, 0.7);
                    text-transform: uppercase;
                    letter-spacing: 1px;
                    margin-bottom: 0.5rem;
                    font-weight: 600;
                }
                
                .widget-content {
                    font-size: 1.1rem;
                    font-weight: 500;
                    color: #333;
                    display: flex;
                    align-items: center;
                    gap: 0.5rem;
                }
                
                .weather-icon {
                    font-size: 1.5rem;
                    color: #667eea;
                }
                
                .date-display {
                    font-size: 1rem;
                    color: #333;
                    font-weight: 500;
                }
                
                @media (max-width: 576px) {
                    .widget-container {
                        flex-direction: column;
                        gap: 0.75rem;
                    }
                }
            </style>

            <div class="container py-5 d-flex align-items-center justify-content-center" style="min-height: 100vh;">
                <div t-attf-class="glass-card {{login_card_classes}} o_database_list" style="max-width: 380px; width: 100%;">
                    <div class="card-body p-4">
                        <div class="text-center pb-4 mb-4">
                            <img t-attf-src="/web/binary/company_logo{{ '?dbname='+db if db else '' }}" alt="Logo" class="glass-logo" style="max-height:100px; max-width: 80%; width:auto; margin-bottom: 10px;"/>
                        </div>
                        
                        <!-- Weather and Date Widgets -->
                        <div class="widget-container">
                            <!-- Dhaka Weather Widget -->
                            <div class="weather-widget">
                                <div class="widget-title">Dhaka Weather</div>
                                <div class="widget-content">
                                    <i class="fa fa-sun-o weather-icon"></i>
                                    <span id="weather-info">Loading...</span>
                                </div>
                            </div>
                            
                            <!-- Today's Date Widget -->
                            <div class="date-widget">
                                <div class="widget-title">Today</div>
                                <div class="widget-content">
                                    <i class="fa fa-calendar date-icon"></i>
                                    <span id="date-display" class="date-display">Loading...</span>
                                </div>
                            </div>
                        </div>
                        
                        <script><![CDATA[
                            // Weather Widget for Dhaka
                            async function loadDhakaWeather() {
                                try {
                                    const response = await fetch('https://api.openweathermap.org/data/2.5/weather?q=Dhaka,BD&units=metric&appid=demo_key');
                                    const data = await response.json();
                                    const temp = Math.round(data.main.temp);
                                    const description = data.weather[0].description;
                                    const icon = data.weather[0].icon;
                                    
                                    const weatherIcons = {
                                        '01d': 'fa-sun',
                                        '01n': 'fa-moon',
                                        '02d': 'fa-cloud-sun',
                                        '02n': 'fa-cloud-moon',
                                        '03d': 'fa-cloud',
                                        '03n': 'fa-cloud',
                                        '04d': 'fa-cloud',
                                        '04n': 'fa-cloud',
                                        '09d': 'fa-cloud-showers-heavy',
                                        '09n': 'fa-cloud-showers-heavy',
                                        '10d': 'fa-cloud-rain',
                                        '10n': 'fa-cloud-rain',
                                        '11d': 'fa-cloud-bolt',
                                        '11n': 'fa-cloud-bolt',
                                        '13d': 'fa-snowflake',
                                        '13n': 'fa-snowflake',
                                        '50d': 'fa-smog',
                                        '50n': 'fa-smog'
                                    };
                                    
                                    const weatherElement = document.getElementById('weather-info');
                                    const iconElement = document.querySelector('.weather-icon');
                                    
                                    weatherElement.innerHTML = `${temp}°C`;
                                    iconElement.className = `fa ${weatherIcons[icon] || 'fa-sun'} weather-icon`;
                                } catch (error) {
                                    // Fallback weather display
                                    const weatherElement = document.getElementById('weather-info');
                                    const iconElement = document.querySelector('.weather-icon');
                                    weatherElement.innerHTML = '25°C';
                                    iconElement.className = 'fa fa-sun weather-icon';
                                }
                            }
                            
                            // Date Widget
                            function updateDate() {
                                const now = new Date();
                                const options = {
                                    weekday: 'long',
                                    year: 'numeric',
                                    month: 'long',
                                    day: 'numeric'
                                };
                                const dateStr = now.toLocaleDateString('en-US', options);
                                document.getElementById('date-display').textContent = dateStr;
                            }
                            
                            // Initialize widgets
                            document.addEventListener('DOMContentLoaded', function() {
                                loadDhakaWeather();
                                updateDate();
                                // Update weather every 10 minutes
                                setInterval(loadDhakaWeather, 600000);
                                // Update date every minute
                                setInterval(updateDate, 60000);
                            });
                        ]]></script>
                        
                        <t t-out="0"/>
                        <div class="text-center small mt-4 pt-3" t-if="not disable_footer">
                            <div class="glass-divider"></div>
<!--                            <t t-if="disable_database_manager">-->
<!--                                <a class="border-end pe-2 me-1" href="/web/database/manager">Manage Databases</a>-->
<!--                            </t>-->
<!--                            <a href="https://www.odoo.com?utm_source=db&amp;utm_medium=auth" target="_blank">Powered by <span>Odoo</span></a>-->
                        </div>
                    </div>
                </div>
            </div>
        </t>
    </template>

    <template id="web.login" name="Login">
        <t t-call="web.login_layout">
            <div class="oe_structure" id="oe_structure_login_top"/>
            <owl-component t-if="not login" name="web.user_switch" />
            <form t-attf-class="oe_login_form #{'' if login else 'd-none'}" role="form" t-attf-action="/web/login" method="post" onsubmit="this.action = '/web/login' + location.hash" data-captcha="login">
                <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>

                <div class="mb-3" t-if="databases and len(databases) &gt; 1">
                    <label for="db" class="glass-label">Database</label>
                    <div class="input-group">
                        <input type="text" name="db" t-att-value="request.db" id="db" class="glass-input form-control" required="required" readonly="readonly"/>
                        <a role="button" href="/web/database/selector" class="glass-button btn btn-sm">Select <i class="fa fa-database" role="img" aria-label="Database" title="Database"></i></a>
                    </div>
                </div>

                <div class="mb-3 field-login">
                    <label for="login" class="glass-label form-label d-flex justify-content-between">Email</label>
                    <input type="text" placeholder="Enter your email" name="login" t-att-value="login" id="login" class="glass-input form-control" required="required" autocapitalize="off" autocomplete="username"/>
                </div>

                <div class="o_caps_lock_warning">
                    <label for="password" class="glass-label form-label d-flex justify-content-between">Password</label>
                    <div class="input-group mb-1">
                        <input type="password" placeholder="Enter your password" name="password" id="password" class="glass-input form-control" required="required" autocomplete="current-password" t-att-autofocus="'autofocus' if login else None" maxlength="4096"/>
                        <button type="button" class="glass-button btn btn-sm o_show_password ms-2" aria-label="Show password" title="Show password">
                            <i class="fa fa-eye" aria-hidden="true"></i>
                        </button>
                    </div>
                </div>

                <p class="alert alert-danger" t-if="error" role="alert">
                    <t t-esc="error"/>
                </p>
                <p class="alert alert-success" t-if="message" role="status">
                    <t t-esc="message"/>
                </p>

                <div class="oe_login_buttons text-center d-grid mb-1 pt-3">
                    <button type="submit" class="glass-button btn btn-primary">Log in</button>
                    <t t-if="debug">
                        <button type="submit" name="redirect" value="/web/become" class="glass-button btn btn-link btn-sm">Log in as superuser</button>
                    </t>
                    <t t-call="web.login_oauth"/>
                </div>

                <input type="hidden" name="type" value="password"/>
                <input type="hidden" name="redirect" t-att-value="redirect"/>
            </form>
            <div class="oe_structure" id="oe_structure_login_bottom"/>
        </t>
    </template>

    <template id="web.login_successful" name="Login successful">
        <t t-call="web.login_layout">
            <div class="oe_login_form glass-card p-4">
                <p>
                    You are logged in.
                </p>
                <div t-attf-class="clearfix oe_login_buttons text-center mb-1 pt-3">
                    <a class="glass-button btn btn-primary btn-block" href="/web/session/logout">Log out</a>
                </div>
            </div>
        </t>
    </template>

    <template id="web.login_oauth" name="Login OAuth">
        <!-- Use auth_btns to insert login options displayed under the
        login form -->
        <t t-set="auth_btns" t-value="[]"/>
        <div t-if="auth_btns" class="o_login_auth">
            <em class="d-block my-3 small text-center text-muted">- or -</em>
            <div class="list-group">
                <a t-foreach="auth_btns" t-as="p" t-attf-class="{{p.get('list_item_class')}} list-group-item list-group-item-action d-flex align-items-center gap-2 py-2" t-att-href="p.get('auth_link') or '#'">
                    <i t-att-class="p['css_class']" role="presentation"/>
                    <span class="mx-auto" t-out="p['body']"/>
                </a>
            </div>
        </div>
    </template>

    <template id="web.test_helpers">
        <t t-call-assets="web.tests_assets" t-js="False"/>
        <style>
            body {
                position: relative; /* bootstrap-datepicker needs this */
            }
            body:not(.debug) .modal-backdrop, body:not(.debug) .modal:not(.o_module_error), body:not(.debug) .ui-autocomplete {
                opacity: 0 !important;
            }
            #qunit-testrunner-toolbar label {
                font-weight: inherit;
                margin-bottom: inherit;
            }
            #qunit-testrunner-toolbar input[type=text] {
                width: inherit;
                display: inherit;
            }
        </style>
        <t t-call-assets="web.tests_assets" t-css="False"/>
    </template>

    <template id="web.unit_tests_suite">
        <t t-call="web.layout">
            <t t-set="html_data" t-value="{'style': 'height: 100%;'}"/>
            <t t-set="title">Web Unit Tests</t>
            <t t-set="head">
                <meta name="viewport" content="width=device-width, initial-scale=1, user-scalable=no"/>

                <script type="text/javascript">
                    odoo.__session_info__ = <t t-out="json.dumps(session_info)"/>;
                </script>

                <t t-call-assets="web.assets_unit_tests_setup" />
                <t t-call-assets="web.assets_unit_tests" defer_load="True" />
            </t>
        </t>
    </template>

    <template id="web.qunit_suite">
        <t t-call="web.layout">
            <t t-set="html_data" t-value="{'style': 'height: 100%;'}"/>
            <t t-set="title">Web Tests</t>
            <t t-set="head">
                <script type="text/javascript">
                    odoo.__session_info__ = <t t-out="json.dumps(session_info)"/>;
                </script>

                <t t-call="web.test_helpers"/>

                <t t-call-assets="web.qunit_suite_tests" t-js="false"/>
                <t t-call-assets="web.qunit_suite_tests" t-css="false"/>
            </t>
            <div id="qunit"/>
            <div id="qunit-fixture"/>
        </t>
    </template>

    <template id="web.webclient_bootstrap">
        <t t-call="web.layout">
            <t t-set="head_web">
                <meta name="viewport" content="width=device-width, initial-scale=1, user-scalable=no"/>
                <meta name="theme-color" content="#71639e"/>
                <link rel="manifest" href="/web/manifest.webmanifest" crossorigin="use-credentials"/>
                <link rel="apple-touch-icon" href="/web/static/img/odoo-icon-ios.png"/>
                <script type="text/javascript">
                    // Block to avoid leaking variables in the script scope
                    {
                        odoo.__session_info__ = <t t-out="json.dumps(session_info)"/>;
                        const { user_context } = odoo.__session_info__;
                        const lang = new URLSearchParams(document.location.search).get("lang");
                        let menuURL = "/web/webclient/load_menus";
                        if (lang) {
                            user_context.lang = lang;
                            menuURL += `&amp;lang=${lang}`;
                        }
                        odoo.reloadMenus = () => fetch(menuURL, { cache: "no-store" }).then(res => res.json());
                        odoo.loadMenusPromise = odoo.reloadMenus();
                    }
                </script>
                <t t-call-assets="web.assets_web_print" media="print" t-js="false"/>
                <t t-call-assets="web.assets_web" t-css="false"/>

                <t t-if="color_scheme == 'dark'">
                    <t t-call-assets="web.assets_web_dark" media="screen" t-js="false"/>
                </t>
                <t t-else="">
                    <t t-call-assets="web.assets_web" media="screen" t-js="false"/>
                </t>
                <t t-call="web.conditional_assets_tests" media="screen"/>
            </t>
            <t t-set="head" t-value="head_web + (head or '')" media="screen"/>
            <t t-set="body_classname" t-value="'o_web_client'"/>
        </t>
    </template>

    <template id="webclient_offline">
        <t t-call="web.layout">
            <t t-set="html_data" t-value="{'style': 'height: 100%;'}"/>
            <t t-set="title">Offline</t>
            <t t-set="head">
                <script type="text/javascript">
                    document.addEventListener("DOMContentLoaded", () => {
                        const cookies = document.cookie.split(';').map(c => c.trim());
                        if (cookies.includes('color_scheme=dark')) {
                            document.body.style.background = "linear-gradient(135deg, #2c3e50 0%, #34495e 25%, #2c3e50 50%, #34495e 75%, #2c3e50 100%)";
                            document.body.style.color = "#FFFFFF";
                        }
                    });
                    window.addEventListener('online', () => location.reload());
                </script>
                <style>
                    body {
                    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Ubuntu, "Noto Sans", Arial, sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol", "Noto Color Emoji";
                    padding:0;
                    margin:0;
                    background: linear-gradient(135deg, #667eea 0%, #764ba2 25%, #f093fb 50%, #feca57 75%, #48c9b0 100%);
                    background-size: 400% 400%;
                    animation: gradientShift 15s ease infinite;
                    color: rgb(17, 24, 39);
                    display: flex;
                    align-items: center;
                    justify-content: center;
                    text-align: center;
                    height: 100vh;
                    width: 100vw;
                    user-select: none;
                    }
                    
                    @keyframes gradientShift {
                        0% { background-position: 0% 50%; }
                        50% { background-position: 100% 50%; }
                        100% { background-position: 0% 50%; }
                    }
                    
                    .card {
                    width: 80%;
                    background: rgba(255, 255, 255, 0.15);
                    backdrop-filter: blur(20px);
                    -webkit-backdrop-filter: blur(20px);
                    border-radius: 20px;
                    border: 1px solid rgba(255, 255, 255, 0.2);
                    box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.37);
                    padding: 2rem;
                    transition: all 0.3s ease;
                    }
                    
                    .card:hover {
                        transform: translateY(-5px);
                        box-shadow: 0 15px 40px 0 rgba(31, 38, 135, 0.45);
                    }
                    
                    .card img {
                    width: 96px;
                    height: auto;
                    filter: drop-shadow(0 4px 8px rgba(0, 0, 0, 0.2));
                    transition: transform 0.3s ease;
                    }
                    
                    .card img:hover {
                        transform: scale(1.05);
                    }
                    
                    .card button {
                    background: linear-gradient(135deg, rgba(102, 126, 234, 0.8), rgba(118, 75, 162, 0.8));
                    color: #FFFFFF;
                    border: 1px solid rgba(255, 255, 255, 0.2);
                    border-radius: 12px;
                    padding: .75rem 1.5rem;
                    cursor: pointer;
                    font-size: 1.2rem;
                    font-weight: 500;
                    backdrop-filter: blur(10px);
                    -webkit-backdrop-filter: blur(10px);
                    transition: all 0.3s ease;
                    position: relative;
                    overflow: hidden;
                    }
                    
                    .card button:hover {
                        background: linear-gradient(135deg, rgba(102, 126, 234, 0.9), rgba(118, 75, 162, 0.9));
                        transform: translateY(-2px);
                        box-shadow: 0 10px 25px rgba(102, 126, 234, 0.4);
                    }
                    
                    .card button::before {
                        content: '';
                        position: absolute;
                        top: 0;
                        left: -100%;
                        width: 100%;
                        height: 100%;
                        background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
                        transition: left 0.5s;
                    }
                    
                    .card button:hover::before {
                        left: 100%;
                    }
                </style>
                <meta name="viewport" content="width=device-width, initial-scale=1"/>
            </t>
            <div class="card">
                <img t-attf-src="data:image/png;base64,{{odoo_icon}}" alt="Odoo logo"/>
                <h1>You are offline</h1>
                <p>Check your network connection and come back here. Odoo will load as soon as you're back online.</p>
                <button onclick="location.reload()">Check again</button>
            </div>
        </t>
    </template>

    <template id="webclient_scoped_app">
        <t t-call="web.layout">
            <t t-set="html_data" t-value="{'style': 'height: 100%;'}"/>
            <t t-set="title" t-value="'Install %s' % (app_name)"/>
            <t t-set="x_icon" t-value="'/scoped_app_icon_png?app_id=%s' % (app_id)" />
            <t t-set="head">
                <t t-call-assets="web.assets_frontend_minimal"/>
                <t t-call-assets="web.assets_frontend_lazy"/>
                <link rel="apple-touch-icon" t-att-href="x_icon"/>
                <link rel="manifest" t-att-href="safe_manifest_url" crossorigin="use-credentials" />
                <meta name="viewport" content="width=device-width, initial-scale=1"/>
            </t>
            <owl-component name="web.install_scoped_app" />
        </t>
    </template>
</odoo>
